# Интеграция MongoDB и графовых моделей данных

**Студент:** Зубикова Злата Павловна  
**Группа:** 5130203/50201  
**Тема:** Integration of MongoDB and Graph Data Models  

---

##  План практики

1. Изучить базовые понятия графовых моделей данных и способы хранения графа в MongoDB
2. Выбрать предметную область и спроектировать граф
3. Написать скрипт для загрузки данных и запросов на Python
4. Провести тестирование производительности
5. Визуализация графа, взвешенные рёбра и поиск кратчайшего пути
6. Оформить отчёт по практике

---

##  Сравнение способов хранения графов в MongoDB

В ходе выполнения первого пункта плана были рассмотрены четыре основных подхода к хранению графовых структур в MongoDB:

| Подход | Описание |
| :--- | :--- |
| **Список смежности (Adjacency List)** | В документе вершины хранится массив ID соседних вершин |
| **Отдельная коллекция рёбер (Edge Collection)** | Вершины в одной коллекции, рёбра — в отдельной. Каждое ребро — документ с полями `from` и `to` |
| **Встраивание рёбер в узел (Embedded Edges)** | Рёбра хранятся как вложенные документы внутри вершины |
| **Материализованные пути (Materialized Paths)** | В документе вершины хранятся предвычисленные пути до других вершин |

### Таблица сравнения

| Критерий | Список смежности (массив ID) | Отдельная коллекция рёбер | Встраивание рёбер в узел | Материализованные пути |
| :--- | :--- | :--- | :--- | :--- |
| **Скорость запроса соседей** | 5/5<br/>Поиск по `_id` мгновенный. Массив извлекается за один запрос. | 4/5<br/>Быстро при индексах на `from`/`to`. Уступает прямому доступу к массиву. | 5/5<br/>Все данные о вершине и рёбрах читаются за один запрос. | 3/5<br/>Пути предвычислены, но требуют парсинга. Глубже обход — медленнее. |
| **Сложность добавления связи** | 4/5<br/>`$push` в массив за один запрос. Нужно обновлять обе вершины для двунаправленных связей. | 4/5<br/>Вставка нового документа в коллекцию `routes`. Немного сложнее, чем `$push`. | 5/5<br/>`$push` во вложенный массив. Самый простой способ. | 2/5<br/>Очень сложно. При добавлении ребра нужно пересчитывать все пути для затронутых вершин. |
| **Возможность хранить свойства рёбер** |  0/5<br/>Невозможно. Массив хранит только ID соседей. |  5/5<br/>Полная поддержка. Можно хранить любые поля: `weight`, `airline`, `price`. |  5/5<br/>Полная поддержка. Вложенные объекты содержат любые поля. |  5/5<br/>Полная поддержка. В объектах путей можно хранить любые поля. |
| **Ограничения** |  Средние<br/>• Массив ограничен 16 МБ<br/>• Нельзя фильтровать рёбра по свойствам<br/>• Для соседей 2 уровня нужны `$lookup` |  Средние<br/>• Для всех соседей нужен `$or` или 2 запроса<br/>• `$graphLookup` медленный на глубине > 3<br/>• Больше места в БД |  Серьёзные<br/>• Лимит 16 МБ на документ<br/>• Сложно обновлять одно ребро<br/>• Сложно искать входящие рёбра |  Критические<br/>• Огромное дублирование данных<br/>• Сложные обновления<br/>• Не масштабируется для графов > 100 узлов |
| **Итоговый балл** | 9/15 + средние ограничения | 13/15 + средние ограничения | 15/15 + серьёзные ограничения | 10/15 + критические ограничения |

---

##  Обоснование выбора подхода

Для реализации проекта выбран подход **«Отдельная коллекция рёбер» (Edge Collection)**.

### Преимущества выбранного подхода:

1. **Хранение свойств рёбер**  
   Поля `weight`, `airline`, `price` могут быть добавлены в документы коллекции `routes`, что критически важно для реализации алгоритма Дейкстры и дальнейшего расширения модели.

2. **Масштабируемость**  
   Отсутствие ограничения на количество рёбер у одной вершины позволяет моделировать граф европейских авиаперелётов любого размера без риска превысить лимит документа в 16 МБ.

3. **Поддержка графовых запросов**  
   Оператор `$graphLookup` позволяет эффективно выполнять рекурсивные запросы для поиска маршрутов с пересадками.

4. **Гибкость**  
   Добавление, удаление и обновление рёбер выполняется стандартными операциями MongoDB без необходимости пересчёта путей (в отличие от материализованных путей, где добавление одного ребра требует обновления множества документов).

### Компенсация недостатков:

Недостаток подхода (необходимость двух запросов для поиска всех соседей) компенсируется:
- Созданием составных индексов по полям `from` и `to`
- Использованием оператора `$or` для объединения запросов

